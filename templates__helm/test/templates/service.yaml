apiVersion: v1
kind: Service
metadata:
  name: {{ template "clusterchat.fullname" . }}
  labels:
    app: {{ template "clusterchat.name" . }}
    chart: {{ template "clusterchat.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "clusterchat.name" . }}
    release: {{ .Release.Name }}
